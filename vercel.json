{
  "framework": "nextjs",
  "buildCommand": "prisma generate && next build",
  "env": {
    "SKIP_ENV_VALIDATION": "1"
  },
  "functions": {
    "app/api/**/*.ts": {
      "runtime": "nodejs18.x"
    }
  }
}